@page "/statistics"
@using MoneyManager.Client.Components.Charts
@using MoneyManager.Client.Components.Charts.ChartCategoryForYear
@using MoneyManager.Client.Services;
@inject ErrorMessage errorMessage
@inject NameOfSubpage nameOfSubpage

<PageTitle>Statistics</PageTitle>

<div class="card mb-2">
    <div class="card-body">
        <ChartCategoryForYear />
    </div>
</div>

<div class="card mb-2">
    <div class="card-body">
        <ChartForAllCategories />
    </div>
</div>

@code {

    private void ErrorMessageSetter(string msg)
    {
        errorMessage.Message = msg;
    }

    protected override void OnInitialized()
    {
        nameOfSubpage.NamesOfSubpage = NamesOfSubpageEnum.Statistics;
    }
}